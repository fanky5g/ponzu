View: []byte(`
            <div class="input-field col s12">
                <label class="active">{{ .Label }}</label>
                <!-- Add your custom editor field view here. -->
            </div>
            `),